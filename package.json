{
  "name": "grocery-bot",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node src/main.js",
    "dev": "node --env-file=.env src/main.js",
    "test": "vitest run",
    "spike:link": "node spike/make-link.mjs",
    "spike:record": "node spike/record.mjs"
  },
  "dependencies": {
    "playwright": "^1.50.0"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}
